---
label: icon-module
hide_body: true
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
- name: icons
  type: field_group_list
  fields:
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
  - name: text
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: true
      schema:
        format: markdown
    label: Text
  config:
    min: 
    max: 
    labelField: 
  label: Icons
